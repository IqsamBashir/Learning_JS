// use const for creating arrays

const fruitsArray=["apple", "mango", "banana", "kiwi"];
fruitsArray.push("orange");  //here we are not changing const array instead we are pushing ther value to that address
console.log(fruitsArray);
// string indexing
let firstName = "iqsam oooldsnsdjhjhsgk";
console.log (firstName);
//to find the character with index no
console.log (firstName[4]);

//to find the string length
console.log(firstName.length);

//to find the last index

console.log (firstName.length-1);

//to find a character from the last index 
console.log(firstName[firstName.length -3 ]);